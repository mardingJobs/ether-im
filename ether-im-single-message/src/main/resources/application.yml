spring:
  sleuth:
    enabled: true
  application:
    name: ether-im-message-single
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: martin.dev:8848
        username: nacos
        password: nacos

server:
  servlet:
    context-path: /im-message-single

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true